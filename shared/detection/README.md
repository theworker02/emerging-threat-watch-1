# Shared Detection Notes

Family-specific rules live under each investigation's `detections/`.

Shared materials here are pattern libraries and review checklists only.
Copying a rule between cases requires re-validation and must not imply cross-family attribution.