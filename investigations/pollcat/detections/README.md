# Detections — pollcat

All rules must include: description, data source, logic, evidence IDs, false positives, limitations, confidence, validation status (EXPERIMENTAL | UNVALIDATED | VALIDATED).

Do not advertise untested rules as production-ready.
Do not import detections from other ETW cases without independent evidence.
